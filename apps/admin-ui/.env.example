# Next.js UI Local Development Configuration
# For running UI locally without Docker (e.g., pnpm dev)
# Copy to ui/.env.local and customize as needed
#
# NOTE: Next.js environment variable rules:
# - NEXT_PUBLIC_* variables are exposed to the browser (client-side)
# - Non-prefixed variables are only available server-side
#
# For Docker deployment, use root-level .env files instead

# =============================================================================
# FHIR Server Configuration
# =============================================================================

# Server-side only (API routes, SSR)
# Used in server components and API routes
FHIR_SERVER_URL=http://localhost:8080

# Client-side (browser)
# Use if you need to make API calls from the browser
# Currently ui/lib/config.ts uses server-side only
# VITE_FHIR_SERVER_URL=http://localhost:8080

# Legacy variable (kept for backwards compatibility)
NEXT_PUBLIC_SERVER_URL=http://localhost:8080

# =============================================================================
# Next.js Configuration
# =============================================================================
NEXT_TELEMETRY_DISABLED=1